# Troubleshooting

- A signed-in user sees the guest price: check authentication and the assigned personal price type.
- A changed price is stale: clear Evolution cache; product data and contextual prices are cached separately.
- After disabling sPricing, sCommerce automatically uses legacy base and promotional prices.
